{
  "name": "zenn",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zenn:start": "zenn preview --host 0.0.0.0",
    "zenn:create": "zenn new:article --slug ${npm_config_slug}"
  },
  "dependencies": {
    "zenn-cli": "^0.4.5"
  }
}
